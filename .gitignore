
# Auditorías de sesión (no commitear)
triggui_*audit*.txt

# Node deps
node_modules/

# Backups locales
*.backup-*
*.tmp-*

# Backups locales pre-migracion (no commitear)
archive/

# 🌒 Archivos de auditoría/handoff temporales (Silicon Valley grade hygiene)
AUDIT_*.txt
TRIGGUI_*HANDOFF*.txt
*.pre-*.bak
*.pre-*.json
.env

# Sanitize backups (no versionar)
*.backup-sanitize-*.json
*.tmp-*
*.bak-audit-*
audit-reports/

# Backups pre-migration de sesiones anteriores (no commitear)
*.bak-pre-*
*.bak-sanitize-*
