{
  "name": "EditionNucleus",
  "description": "Núcleo canónico de una edición Triggui. Triggui es un disparador: su único objetivo es que el lector abra un libro físico. Para lograrlo, el modelo produce texto que suena como si el lector acabara de abrir ese libro en una página al azar — la voz del libro, el tono del autor, el ritmo del libro. El modelo NO es crítico ni reseñista: no habla DEL libro, produce texto que podría estar EN el libro. El output se descarta si contiene frases tipo 'este libro te invita', 'el autor propone', 'a través de esta obra'. La curaduría silenciosa (lens) es una lente con la que el curador está viendo el mundo hoy: si la lente resuena con ESTE libro específico, el modelo elige la parte del libro que activa esa lente; si no resuena, el modelo marca lens_relevance=false y procede como si la lente no existiera.",
  "strict": true,
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "book_identity",
      "book_grounding_anchors",
      "lens_analysis",
      "card_es",
      "card_en",
      "emotional_words_es",
      "emotional_words_en",
      "og_phrases_es",
      "og_phrases_en",
      "edition_phrases_es",
      "edition_phrases_en",
      "visual_signature",
      "surface_hints",
      "lens_relevance",
      "confidence"
    ],
    "properties": {
      "book_identity": {
        "type": "object",
        "additionalProperties": false,
        "required": ["titulo_es", "titulo_en", "autor", "idioma_original"],
        "properties": {
          "titulo_es": { "type": "string" },
          "titulo_en": { "type": "string" },
          "autor": { "type": "string" },
          "idioma_original": { "type": "string", "enum": ["es", "en"] }
        }
      },

      "book_grounding_anchors": {
        "type": "object",
        "description": "CAMPO CRÍTICO DE ANCLAJE. Antes de producir cualquier otra cosa, llena este campo. Estos son los anclajes reales del libro — conceptos, términos, argumentos, ejemplos específicos que SOLO aparecen en ESTE libro escrito por ESTE autor. Si no puedes nombrar 3 conceptos específicos del libro, marca book_known=false y baja confidence. Si puedes, nómbralos con precisión. DESPUÉS, cada frase que produzcas (card_es, card_en, og_phrases, edition_phrases) debe usar al menos un anchor. Si una frase pudiera aparecer tal cual en cualquier libro de autoayuda genérica, la estás haciendo mal: regresa a los anchors y reescríbela. Anti-patrones a detectar en tu propio output: 'danza de decisiones', 'laberinto de la existencia', 'sueños anhelados', 'búsqueda incesante', 'esencia pura', 'horizonte de posibilidades' — esas metáforas huecas son señal de que perdiste el libro y caíste en vocabulario de post de Instagram. Eso NO es lo que escribiría el autor real.",
        "additionalProperties": false,
        "required": ["book_known", "concepts", "key_terms", "authorial_voice_notes"],
        "properties": {
          "book_known": { "type": "boolean", "description": "true si conoces genuinamente el libro. false si solo tienes conocimiento superficial por título/autor." },
          "concepts": {
            "type": "array",
            "description": "3-5 conceptos, ideas, o argumentos específicos que aparecen en ESTE libro. Deben ser cosas que un lector del libro reconocería, no genéricos. Ejemplo para 'Atomic Habits' de James Clear: ['regla 1% mejor cada día', 'hábitos atómicos como células de cambio', 'loop señal-antojo-respuesta-recompensa', 'identidad vs resultados']. Ejemplo para 'La razón de ser feliz' de Kaushik Basu: ['función de felicidad comparativa', 'dilema del prisionero aplicado a decisiones personales', 'economía del bienestar subjetivo', 'teoría de juegos y elección racional']. Si book_known=false, lista los conceptos más probables dados el título y autor, y baja confidence.",
            "items": { "type": "string", "minLength": 10, "maxLength": 120 }
          },
          "key_terms": {
            "type": "array",
            "description": "3-6 palabras o frases cortas del vocabulario propio del autor en ESTE libro. Términos que el autor usa de forma característica. No palabras genéricas de autoayuda.",
            "items": { "type": "string", "minLength": 2, "maxLength": 40 }
          },
          "authorial_voice_notes": {
            "type": "string",
            "description": "2-3 oraciones describiendo CÓMO escribe este autor en ESTE libro específico: tono, densidad, rítmica, si usa ejemplos cotidianos o abstractos, si es imperativo o reflexivo, etc. Este análisis guía las cards y phrases. Ejemplo: 'Basu combina humor académico con precisión económica. Frases cortas, ejemplos personales mezclados con teoría formal, evita jerga sin explicarla.'",
            "minLength": 50,
            "maxLength": 400
          }
        }
      },

      "lens_analysis": {
        "type": "object",
        "description": "CAMPO DE ANÁLISIS OBLIGATORIO antes de decidir si la lente del curador aplica. Si se pasó lente (lens o book_context), PRIMERO analizas cómo se relaciona con el libro, DESPUÉS decides si aplica. Si no se pasó lente, analysis='(sin lente)' y applied=false. Regla clave: NO puedes marcar applied=false sin antes haber buscado genuinamente cómo el libro trata el tema. Solo si tras buscar no encuentras conexión real, marcas applied=false con la razón específica.",
        "additionalProperties": false,
        "required": ["lens_provided", "analysis", "decision"],
        "properties": {
          "lens_provided": { "type": "boolean", "description": "true si el curador pasó una lente." },
          "analysis": {
            "type": "string",
            "description": "Si lens_provided=true: 2-4 oraciones explicando CÓMO el libro trata el tema de la lente. Cita conceptos específicos de book_grounding_anchors. Si el libro genuinamente no trata el tema, explícalo con precisión ('Este libro trata X, no Y. El tema más cercano sería Z'). Si lens_provided=false: '(sin lente provista)'.",
            "minLength": 10,
            "maxLength": 600
          },
          "decision": {
            "type": "string",
            "enum": ["apply_directly", "apply_through_adjacent_concept", "dont_apply_book_is_about_something_else"],
            "description": "apply_directly: el libro trata directamente el tema de la lente, extracción se sesga hacia ahí. apply_through_adjacent_concept: el libro no trata el tema pero hay un concepto cercano que resuena, extracción usa ese puente. dont_apply_book_is_about_something_else: el libro no tiene conexión genuina con la lente, extracción procede normal."
          }
        }
      },

      "card_es": {
        "type": "object",
        "description": "Los 4 campos de la tarjeta en español. Cada uno debe sonar como si fuera texto del libro mismo, en la voz del autor en ESTE libro. NO uses 'este libro', 'el autor', 'la obra', 'a través de', 'nos invita a', 'reflexiona sobre', 'trata de'. Habla como habla el libro.",
        "additionalProperties": false,
        "required": ["titulo", "parrafoTop", "subtitulo", "parrafoBot"],
        "properties": {
          "titulo": { "type": "string", "description": "Título de la tarjeta. Corto, denso. Puede ser una frase del libro o una formulación que el autor usaría. 3-8 palabras." },
          "parrafoTop": { "type": "string", "description": "Primer párrafo. Debe sonar como abrir el libro en esa página. Densidad alta. 25-60 palabras." },
          "subtitulo": { "type": "string", "description": "Pregunta o frase puente. Debe estar en la voz del libro. 6-14 palabras." },
          "parrafoBot": { "type": "string", "description": "Párrafo de cierre. NO instrucciones de tiempo ('toma X segundos'), NO 'después de', NO envolturas narrativas. Texto directo del libro. 25-60 palabras." }
        }
      },

      "card_en": {
        "type": "object",
        "description": "The 4 card fields in English. Native English, same voice as the book's author in this book. Do NOT write like a reviewer. Write like the book itself.",
        "additionalProperties": false,
        "required": ["titulo", "parrafoTop", "subtitulo", "parrafoBot"],
        "properties": {
          "titulo": { "type": "string" },
          "parrafoTop": { "type": "string" },
          "subtitulo": { "type": "string" },
          "parrafoBot": { "type": "string" }
        }
      },

      "emotional_words_es": {
        "type": "array",
        "description": "4 palabras emocionales específicas a ESTE libro. Palabras que emergen del libro mismo, no palabras genéricas de autoayuda.",
        "items": { "type": "string" }
      },

      "emotional_words_en": {
        "type": "array",
        "items": { "type": "string" }
      },

      "og_phrases_es": {
        "type": "array",
        "description": "4 frases cortas y densas para el OG image (headline del preview de WhatsApp). OBJETIVO: 40-70 caracteres por frase (incluyendo emoji al INICIO). Son el gancho que ve la gente antes de decidir abrir el link. Deben ser punzantes, autocontenidas, imperativas o declarativas con peso. Ejemplos del tono correcto: '🎲 Cada movimiento cambia el tablero entero' (44 chars), '⚓ La felicidad vive en la jugada, no en el premio' (51 chars), '♟ Lo esencial es jugar, no ganar' (33 chars). Prohibidas frases genéricas tipo 'La vida es un juego' (podría ser cualquier libro). Deben sentirse como si fueran del libro mismo, no reseña del libro.",
        "items": {
          "type": "string",
          "minLength": 35,
          "maxLength": 74
        }
      },

      "og_phrases_en": {
        "type": "array",
        "description": "4 short dense phrases for OG image headline. 40-70 characters target including emoji at START. Same rule as og_phrases_es but in native English.",
        "items": {
          "type": "string",
          "minLength": 35,
          "maxLength": 74
        }
      },

      "edition_phrases_es": {
        "type": "array",
        "description": "4 frases de tono reflexivo, densas y más largas, para los 4 bloques grandes de la Edición Viva. OBJETIVO: 80-108 caracteres por frase (incluyendo emoji al INICIO). REGLA CRÍTICA: cada frase DEBE terminar en punto, signo de interrogación, signo de admiración, o cierre natural ANTES del carácter 112. Si una frase no cabe completa, NO la empieces — piensa una frase más corta que sí cierre. PROHIBIDO dejar frases colgando tipo '...la felicidad se encuentra en a' (truncada). Cada frase debe usar al menos un concepto de book_grounding_anchors.concepts o un término de key_terms. Ejemplo correcto: '🦋 Observa el cielo durante unos instantes, sintiendo la vastedad del universo a tu alrededor.' (95 chars, cierra con punto). El lector debe poder leerla y sentir que ya empezó a leer el libro.",
        "items": {
          "type": "string",
          "minLength": 70,
          "maxLength": 112
        }
      },

      "edition_phrases_en": {
        "type": "array",
        "description": "4 longer reflective phrases for the 4 grand blocks of the Living Edition. TARGET: 80-108 characters including emoji at START. CRITICAL RULE: each phrase MUST end with period, question mark, exclamation, or natural closure BEFORE character 112. Do NOT start phrases that cannot close. Forbidden: phrases that get truncated mid-word. Each phrase must use at least one concept from book_grounding_anchors.concepts or one term from key_terms. Native English prose.",
        "items": {
          "type": "string",
          "minLength": 70,
          "maxLength": 112
        }
      },

      "visual_signature": {
        "type": "object",
        "description": "Firma visual derivada del libro y su autor. Esto NO es decoración arbitraria: es la identidad visual que emerge del libro mismo. Dos libros distintos deben tener firmas radicalmente distintas; dos extracciones del mismo libro deben tener firmas casi idénticas.",
        "additionalProperties": false,
        "required": [
          "palette",
          "accent",
          "paper",
          "ink",
          "typography_family",
          "density",
          "temperature",
          "rhythm",
          "era",
          "genre_visual"
        ],
        "properties": {
          "palette": {
            "type": "array",
            "description": "4 colores hex coherentes con el tono emocional del libro. No paleta del logo, no paleta Triggui: paleta DEL LIBRO.",
            "items": { "type": "string" }
          },
          "accent": { "type": "string", "description": "Color acento principal. Hex." },
          "paper": { "type": "string", "description": "Color de fondo. Hex. Puede ser claro o oscuro según el libro." },
          "ink": { "type": "string", "description": "Color de tinta. Hex. Debe contrastar con paper." },
          "typography_family": {
            "type": "string",
            "description": "Familia tipográfica sugerida según voz del autor.",
            "enum": ["serif_clasico", "serif_literario", "serif_moderno", "sans_geometrico", "sans_humanista", "sans_tecnologico", "mono_precision", "mixta_editorial"]
          },
          "density": {
            "type": "string",
            "description": "Densidad tipográfica emergente del autor. Tolle es aireado; Dostoievski es denso.",
            "enum": ["aireado", "equilibrado", "denso"]
          },
          "temperature": {
            "type": "string",
            "description": "Temperatura emocional dominante del libro.",
            "enum": ["frio", "neutro", "calido", "ardiente"]
          },
          "rhythm": {
            "type": "string",
            "description": "Ritmo de prosa del autor en ESTE libro.",
            "enum": ["lento", "medio", "rapido", "staccato"]
          },
          "era": {
            "type": "string",
            "description": "Época estética del libro. Puede ser distinta a la fecha de publicación.",
            "enum": ["clasico", "moderno", "contemporaneo", "atemporal"]
          },
          "genre_visual": {
            "type": "string",
            "description": "Género visual que encaja con este libro.",
            "enum": ["academico", "literario", "manifiesto", "poesia", "narrativa", "ensayo", "tecnico", "espiritual"]
          }
        }
      },

      "surface_hints": {
        "type": "object",
        "additionalProperties": false,
        "required": ["dimension", "punto_hawkins", "franja_ideal"],
        "properties": {
          "dimension": { "type": "string", "enum": ["Bienestar", "Prosperidad", "Conexion"] },
          "punto_hawkins": { "type": "string", "enum": ["Cero", "Creativo", "Activo", "Maximo"] },
          "franja_ideal": { "type": "string", "enum": ["madrugada", "manana", "tarde", "noche"] }
        }
      },

      "lens_relevance": {
        "type": "object",
        "description": "Resumen derivado de lens_analysis para consumo del orquestador. applied=true cuando lens_analysis.decision es apply_directly o apply_through_adjacent_concept. applied=false solo cuando decision=dont_apply_book_is_about_something_else o cuando no hubo lente. La razón debe ser breve, interna, no aparece en output al usuario.",
        "additionalProperties": false,
        "required": ["applied", "reason"],
        "properties": {
          "applied": { "type": "boolean" },
          "reason": { "type": "string", "description": "Resumen breve de lens_analysis.analysis. Máx 200 chars.", "maxLength": 200 }
        }
      },

      "confidence": {
        "type": "object",
        "additionalProperties": false,
        "required": ["book_grounding", "voice_authenticity", "specificity"],
        "properties": {
          "book_grounding": { "type": "number", "description": "0-1. Qué tan bien conoce el modelo este libro específico." },
          "voice_authenticity": { "type": "number", "description": "0-1. Qué tan cerca está el output de la voz real del autor en este libro." },
          "specificity": { "type": "number", "description": "0-1. Qué tan específico al libro es el output. Bajo = podría ser cualquier libro." }
        }
      }
    }
  }
}
